name: 'kba_kelompok_7'
version: '1.0.0'
config-version: 2

# nama profil koneksi
profile: 'kba_clickhouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  kba_kelompok_7:
    silver:
      +materialized: table
      tags: ["silver"]
    gold:
      +materialized: table
      +schema: gold
      tags: ["gold"]